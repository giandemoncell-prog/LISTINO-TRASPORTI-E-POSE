# Sessione 15 maggio 2026 — Riepilogo e prossimi passi

## Cosa è stato fatto

### Bug fix (team posa-bugfix)
Analisi automatica + fix applicati a `index.html`:

| # | Bug | Riga | Fix |
|---|-----|------|-----|
| 1 | `isPro()` crash su null/undefined | ~1124 | try/catch + controllo `=== '1'` |
| 2 | Play Billing senza error handling | ~1195–1216 | try/catch su `initPlayBilling()` e `aggiornaStatoAbbonamento()` |
| 3 | Prezzi negativi nel preventivo | ~856 | `if (prezzo < 0) prezzo = 0` |
| 4 | Quantità zero/NaN | ~850 | `qtaRaw > 0 ? qtaRaw : 1` |
| 5 | IVA negativa/zero | ~842 | `ivaRaw > 0 ? ivaRaw : 0` |
| 6 | `caricaPreventivo()` con indici fragili | ~1066 | usa `data-linea` al posto degli indici forEach |
| 7 | Scroll mancante al cambio tab | ~772 | `window.scrollTo(0, 0)` in `showPanel()` |
| 8 | Newline nei campi cliente/indirizzo | ~903–905 | `.replace(/[\r\n]+/g, ' ')` in `buildTestoPreventivo()` |

### Versione aggiornata
- `versionCode` 2 → **3**
- `versionName` 1.0.1 → **1.0.2**
- File: `android/app/build.gradle`

### Upload Play Console
- AAB 1.0.2 caricato sul canale **Test chiusi (Alpha)**
- Stato: inviato per revisione Google
- Data: 15 maggio 2026

---

## Stato attuale Play Console

| Canale | Versione | Stato |
|--------|----------|-------|
| Test interni | 1.0.1 | Attivo |
| Test chiusi Alpha | **1.0.2** | In revisione |
| Produzione | — | Bloccata (vedi sotto) |

### Requisito per sbloccare la Produzione
Google richiede:
- **Almeno 12 tester** con test chiuso attivato (attualmente: 6)
- **Almeno 14 giorni** di test chiuso continuativo

→ **Servono altri 6 tester.** Devono solo avere un account Gmail, accettare l'invito e installare l'app una volta.

---

## Prossimi passi

1. **Aggiungere 6 tester** al test chiuso Alpha (Play Console → Test chiusi → Alpha → Gestisci tester)
2. **Aspettare 14 giorni** dal momento in cui si raggiungono 12 tester attivi
3. **Richiedere accesso alla produzione** (il tasto si sblocca automaticamente)
4. Google revisiona la domanda → di solito 3–7 giorni lavorativi
5. Dopo approvazione: caricare l'AAB in produzione e pubblicare

---

## Bug trovati ma NON ancora fixati (bassa priorità)

- `formatEuro()` arrotondamento non bancario (riga ~895)
- `resetPreventivo()` lascia linee fantasma (riga ~1007)
- Storico: nessun limite memoria localStorage (riga ~973)
- `manifest.json`: array screenshots vuoto
- `acquistaMensile/Annuale()`: `product.getOffer()` non gestito se undefined
- Nessun controllo localStorage disponibile in modalità incognito

---

## File chiave

| File | Descrizione |
|------|-------------|
| `index.html` | App completa (CSS + JS inline) |
| `android/app/build.gradle` | Versione app (versionCode/versionName) |
| `android/app/build/outputs/bundle/release/app-release.aab` | AAB firmato pronto per upload |
| `ISTRUZIONI_PROSEGUIRE.txt` | Guida pubblicazione step-by-step |
| `KEYSTORE_README.md` | Info chiave di firma (non perdere!) |
